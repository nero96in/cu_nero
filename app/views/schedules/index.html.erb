<div class="container">
  <div class="post__header"></div>
  <!--방문자 수: <%#= @total_view_count %> -->
  <hr/>
  <br/>
  <%= month_calendar events: @schedules do |date, meetings| %>
    <%= date.day %>
  
    <% meetings.each do |meeting| %>
      <div>
        <%= meeting.name %>
      </div>
    <% end %>
  <% end %>
</div>



<%= link_to 'New Schedule', new_schedule_path %>
